/** @type {import('next').NextConfig} */
const nextConfig = {};

export default {
    images: {
      domains: ['media.licdn.com', 'upload.wikimedia.org', 'seeklogo.com', 'cdn.freebiesupply.com'],
    },
  };